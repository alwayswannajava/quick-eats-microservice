server:
  port: 8081
mongock:
  migration-scan-package: com.user.service.migration
  enabled: true
spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
      password: ${MONGODB_PASSWORD}
      database: users